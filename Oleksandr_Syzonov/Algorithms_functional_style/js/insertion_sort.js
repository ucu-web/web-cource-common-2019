import {conditional_swap} from "./bubble_sort";



//generating a random  array of 40 numbers
let arr = Array.from({length: 40}, () => Math.floor(Math.random() * 40));


function conditional_swap_recursive(array, cur_ind) {
    return cur_ind === 0 ? array : conditional_swap(arr, )
}