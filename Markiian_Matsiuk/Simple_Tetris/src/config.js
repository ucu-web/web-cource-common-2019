export const blockSize = 20;
export const spaceBetweenBlocks = 3;

export const height = Math.floor(window.innerHeight / (blockSize + spaceBetweenBlocks)) - 1;
export const width = Math.floor(window.innerWidth / (blockSize + spaceBetweenBlocks)) - 1;
